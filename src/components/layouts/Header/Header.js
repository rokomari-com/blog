import React from 'react';
import './Header.scss';

const Header = () => {
    return (
        <header>
            <h1>
                <a href="/">Rokomari Blog</a>
            </h1>
        </header>
    );
};

export default Header;